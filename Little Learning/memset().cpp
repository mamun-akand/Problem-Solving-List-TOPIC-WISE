#include<bits/stdc++.h>
using namespace std;

int main(){
    char s[25];
    char ch = 'a';
                          // Only works with char type array. (i.e., char s[25])
    memset(s, ch, 20);   // memset(destination, character, count);
    cout << s << endl;

    return 0;
}


































